[project]
name = "radarpulse"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    pydantic>=2.0.0,
trafilatura>=1.6.0,
playwright>=1.40.0,
redis>=5.0.0,
python-dotenv>=1.0.0
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "black>=24.0.0",
]